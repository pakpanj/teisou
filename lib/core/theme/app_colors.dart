import 'package:flutter/material.dart';

/// Design tokens sampled from the Kana Master mockups.
class AppColors {
  AppColors._();

  static const background = Color(0xFFFFFFFF);
  static const primaryCoral = Color(0xFFF4667A);
  static const secondaryBlue = Color(0xFF4F97F4);
  static const tertiaryAmber = Color(0xFFE8B84B);
  static const tertiaryAmberCardBg = Color(0xFFFCEEDB);
  static const textNavy = Color(0xFF1E2A47);
  static const successGreen = Color(0xFF4CAF6E);
  static const errorRed = Color(0xFFE5484D);
  static const cardWhite = Color(0xFFFDF9FA);

  /// Pale pink background used behind the "Belajar Hiragana" menu card.
  static const hiraganaCardBg = Color(0xFFFCE9EC);

  /// Pale blue background used behind the "Belajar Katakana" menu card.
  static const katakanaCardBg = Color(0xFFE8F0FE);

  /// Gold gradient for the PREMIUM tier badge.
  static const premiumGoldStart = Color(0xFFF6D365);
  static const premiumGoldEnd = Color(0xFFC9A227);

  /// Grey used for the FREE tier badge and "Segera Hadir" chips.
  static const freeBadgeGrey = Color(0xFF9AA3AF);
}
