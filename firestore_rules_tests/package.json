{
  "name": "firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Real Firestore Rules emulator tests for firestore.rules — see firestore_rules_tests/README.md. Separate from functions/ so this dev-only dependency never ships with Cloud Functions.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.6.0"
  }
}
