<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Permissions pulled in by plugins and taken back out again.
         ============================================================
         These are not declared here; they arrive through manifest merging
         from the `camera` and `google_mobile_ads` plugins, and every one
         of them was in the shipped manifest without a single line of app
         code using it. For an app aimed at children that is worth more
         than tidiness: a reviewer sees a kana-learning app asking to
         record audio, and Google's Families policy forbids the
         advertising ID outright.

         RECORD_AUDIO / storage: dragged in by the `camera` plugin. The
         microphone feature was deleted when Kaiwa moved to
         image-and-multiple-choice, and nothing has picked or written a
         file since — there is no image picker in the dependency list at
         all.

         AD_ID and the ad-services pair: dragged in by google_mobile_ads.
         The app already configures AdMob as child-directed at runtime
         (see AdAudience); carrying the permission as well is the thing
         Play's Advertising ID declaration asks about, and the answer for
         a children's app has to be that it is not used.

         CAMERA: the only one the app has code for — but Cam Detector is
         locked out of navigation while its bugs are open, so today
         nothing can reach it. **Delete the CAMERA line below when Cam
         Detector is switched back on in ModulesScreen**, or the screen
         will be denied the permission it asks for. -->
    <uses-permission android:name="android.permission.CAMERA" tools:node="remove"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove"/>
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove"/>
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID" tools:node="remove"/>
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION" tools:node="remove"/>
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_TOPICS" tools:node="remove"/>

    <!-- required="false" so devices without a camera can still install;
         CamDetectorScreen shows an error state instead. -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <application
        android:label="Kana Master"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Impeller's camera-texture fence handling is broken on
                 Android API < 33 (confirmed on a physical Android 12
                 device): CameraController initializes and streams frames
                 fine, but CameraPreview renders solid black. Falls back to
                 Skia, which doesn't have this bug. Re-test with Impeller
                 enabled next time minSdk moves past 33, in case upstream
                 fixes it — no other part of the app depends on Impeller. -->
            <meta-data
              android:name="io.flutter.embedding.android.EnableImpeller"
              android:value="false"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Real AdMob App ID for "Teisou Kana Master" (Android). The iOS
             app is a *different* app in the console with its own id, in
             ios/Runner/Info.plist — this account also holds an unrelated
             app called Cash Teisou, so check the platform and the app name
             before pasting anything here.

             Ad *unit* ids in ad_service.dart are still Google's test ones,
             and should stay that way until release: real units fired during
             development count as invalid traffic and can get the account
             suspended. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-7168330620893919~3107201564" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
