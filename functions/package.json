{
  "name": "functions",
  "description": "Cloud Functions for Teisou — sends a push notification (FCM) the moment a chat or clan message is written, since the Flutter app itself has no way to notify a learner whose app isn't open.",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "lint": "true"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  },
  "private": true
}
