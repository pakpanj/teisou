# Loaded automatically by the Firebase CLI at deploy time for all
# functions in this codebase (2nd-gen). Not a secret — a plain feature
# flag, not a key — see functions/iap.js's playConfigured() doc comment
# for what it gates and why it fails closed while unset.
PLAY_VERIFICATION_ENABLED=true
